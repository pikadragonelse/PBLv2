import React, { useEffect, useState, useRef } from 'react';

export type useRequestFirebase = { childLevel1?: string; childLevel2?: string };

export const useRequestFirebase = ({ childLevel1 = '', childLevel2 = '' }: useRequestFirebase) => {
    try {
    } catch (error) {
        console.log(error);
    }
};
